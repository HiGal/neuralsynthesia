# Web Framework
Flask==3.0.0
Flask-Cors==4.0.0
Werkzeug==3.0.1

# HTTP Requests
requests==2.31.0

# Translation (optional, for translating Russian to English)
translators==5.9.1

# Environment Variables
python-dotenv==1.0.0

# Audio processing (for Yandex STT/TTS)
# Note: ffmpeg must be installed separately on the system
